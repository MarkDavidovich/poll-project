# Frontend

The frontend is intentionally small:

- `App.jsx` sets up the two routes
- `pages/CreatePoll.jsx` handles poll creation
- `pages/PollView.jsx` handles voting and results
- `utils/api.js` contains all backend requests

The goal is to keep the request flow easy to follow during interviews and code reviews.
